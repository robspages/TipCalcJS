




function tipContext ()
{
	this.serviceLevels = []; 
	this.isFlatRate = false; 

	this.addServiceLevel(level, rate)
	{
		this.serviceLevels.push({label:level, rate}); 
	}

}




 atcCtrl($scope){
 	$scope.serviceLevels = new tipContext; 
 	$scope.addServiceLevel = function(name, levels)
	$scope.Calc = function()
		{

		};
}
